type prob

val sample : 'a Distribution.t * string ~D~> 'a
val sample' : prob * ('a Distribution.t * string) -AD-> 'a
val factor : float ~D~> unit
val factor' : prob * float -AD-> unit
val observe : 'a Distribution.t * 'a ~D~> unit
val observe' : prob * ('a Distribution.t * 'a) -AD-> unit

type fm_params = {
  fm_particles: int;
  fm_is_particles: int;
}

val infer :
  fm_params -S-> ('a ~D~> 'b * 'c) -S-> 'a -D-> ('b * 'c) Distribution.t
